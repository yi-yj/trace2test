[project]
name = "tracetotest-browser-use-runtime"
version = "0.1.0"
description = "Isolated Browser Use runtime for Trace2Test"
requires-python = ">=3.11,<3.13"
dependencies = [
    "browser-use==0.13.10",
    "pyyaml>=6.0,<7",
    "python-dotenv>=1.0,<2",
]
